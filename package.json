{
	"name": "gist-manager",
	"version": "1.0.0",
	"private": true,
	"packageManager": "yarn@1.22.19",
	"workspaces": [
		"client",
		"server"
	],
	"scripts": {
		"install:all": "yarn install",
		"dev": "concurrently \"yarn workspace gist-manager-server start\" \"yarn workspace gist-manager-client start\"",
		"build": "yarn workspace gist-manager-client build && cp -r client/build server/",
		"start": "yarn workspace gist-manager-server start",
		"clean": "yarn workspace gist-manager-client run clean && rm -rf node_modules server/build",
		"test": "yarn workspace gist-manager-client test"
	},
	"devDependencies": {
		"concurrently": "^9.2.0"
	}
}
